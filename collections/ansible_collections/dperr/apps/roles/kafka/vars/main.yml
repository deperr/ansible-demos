---
# vars file for dperrone.apps.kafka (derived values)

kafka_tarball_name: "kafka_{{ kafka_scala_version }}-{{ kafka_version }}.tgz"
kafka_extract_dir: "kafka_{{ kafka_scala_version }}-{{ kafka_version }}"
kafka_download_url: "https://archive.apache.org/dist/kafka/{{ kafka_version }}/{{ kafka_tarball_name }}"
